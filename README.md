# js_with_tech_gun

## This is for practice purposes

```bash

#We have two keywords for loading the js file.

- async ---> loads the javascript parallerly. i.e when js tag occurs
- defer ---> load the javascript after the full html file load.
```

```bash
# We have three types of Pop ups i.e

- alert ----> Shows an alert message.
- prompt ---> It takes a value from the user , using prompt box.
- confirm ---> It is a confirm box.

- prompt takes two values i.e your question , default value .....like
    - prompt('Enter your age ? ', 20);
```
